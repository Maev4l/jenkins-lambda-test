pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                checkout scm

                echo 'Hello world 57!'

                sh 'df -h'
            }
        }
    }
}